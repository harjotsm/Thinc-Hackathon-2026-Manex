// Stub for `server-only` in vitest. The real package throws an error when
// imported outside the Next.js server runtime, which breaks unit tests.
// Aliased via vitest.config.ts so tests can import server-only modules safely.
export {};
